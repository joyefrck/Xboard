# Windows 1.6.8 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce the verified Windows x64 installer for `1.6.8+10608` with the established filename `ElephantNetwork-Setup-x64-v1.6.8.exe`.

**Architecture:** Keep application behavior unchanged and update only the Windows release metadata contract. Build through the existing Windows GitHub Actions workflow so Flutter, Go, native C++, Inno Setup, service installation, and uninstall cleanup are exercised on Windows Server 2022.

**Tech Stack:** Flutter, Dart, Go, C++, Inno Setup 6, GitHub Actions Windows runner.

---

### Task 1: Lock the Windows 1.6.8 version contract

**Files:**
- Modify: `test/windows_distribution_contract_test.dart`

- [ ] **Step 1: Change the release expectations to 1.6.8**

Expect `#define AppVersion "1.6.8"`, `#define AppBuild "10608"`, and `version: 1.6.8+10608`.

- [ ] **Step 2: Run the focused test and confirm it fails against 1.6.7**

Run: `flutter test test/windows_distribution_contract_test.dart --plain-name "Windows installer has stable identity and lifecycle cleanup"`

Expected: FAIL because the release sources still contain `1.6.7+10607`.

### Task 2: Update the release sources

**Files:**
- Modify: `pubspec.yaml`
- Modify: `windows/installer/ElephantNetwork.iss`
- Modify: `.github/workflows/windows-client.yml`
- Modify: `docs/windows-release.md`

- [ ] **Step 1: Set all Windows defaults to 1.6.8+10608**

Use release version `1.6.8`, numeric build `10608`, and installer filename `ElephantNetwork-Setup-x64-v1.6.8.exe`.

- [ ] **Step 2: Run the focused version contract test**

Run: `flutter test test/windows_distribution_contract_test.dart --plain-name "Windows installer has stable identity and lifecycle cleanup"`

Expected: PASS.

### Task 3: Verify and package on Windows

**Files:**
- Verify: all modified release files
- Produce: `build/releases/windows/1.6.8/ElephantNetwork-Setup-x64-v1.6.8.exe`

- [ ] **Step 1: Run local release checks**

Run `flutter analyze`, `flutter test --no-pub`, `go test ./...`, `go test -race ./...`, and `git diff --check`; require exit code 0.

- [ ] **Step 2: Commit and push the scoped release update**

Commit only the version contract and release plan, then push the current `master` branch to `origin`.

- [ ] **Step 3: Run and wait for Windows CI**

Dispatch `.github/workflows/windows-client.yml` with `version=1.6.8` and `build_number=10608`; require the Windows Release build, protocol tests, installer build, silent install, LocalSystem service check, uninstall, and cleanup stages to pass.

- [ ] **Step 4: Download and verify the installer**

Download the CI artifact, place the `.exe` in `build/releases/windows/1.6.8`, record file size and SHA-256, and report that Authenticode signing remains disabled.
